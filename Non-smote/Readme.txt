1. To featurize data run "featurization.R". You will be asked to choose to select a file. Choose the file having sgRNA sequences. Make sure to name the column to "x30mer".
   Generated features are saved in "featuredataAll" variable. Run "write.csv(featuredataAll,"filename.csv")" to save this data in csv format for later use.

2. 